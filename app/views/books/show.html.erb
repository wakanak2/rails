    <% if flash[:notice] %>
      <div class="flash">
        <%= flash[:notice] %>
      </div>
    <% end %>


<h5>Title</h5>
<%= @book.title %>

<h5>Body</h5>
<%= @book.body %>

<%= link_to "edit",edit_book_path(@book.id) %>
<%= link_to "Back",books_path(@books) %>


